# 🚀 Fixed: Using Direct Connection (Port 5432) to bypass Render's SNI proxy issue
spring.datasource.url=jdbc:postgresql://db.retcmghgasxntyyjdulx.supabase.co:5432/postgres?sslmode=require

# 🔑 For Direct Connections, the username is just 'postgres'
spring.datasource.username=postgres
spring.datasource.password=Zaceinsteinzac481899

# ⚡ Keep your HikariCP settings as they are
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.idle-timeout=300000

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# 🛠️ Set this to update so Hibernate can safely build your portfolio tables directly
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true